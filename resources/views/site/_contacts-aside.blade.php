<div class="contacts-aside-block">
    <div class="text-center">
        @lang('general.call')
    </div>
    <hr class="separator">
    <ul class="phones text-center">
        <li><i class="fas fa-phone"></i><a href="tel:{{ config('contacts.mobil') }}">{{ config('contacts.mobil') }}</a></li>
        <li><i class="fas fa-phone"></i><a href="tel:{{ config('contacts.phone-1') }}">{{ config('contacts.phone-1') }}</a></li>
        <li><i class="fas fa-phone"></i><a href="tel:{{ config('contacts.phone-2') }}">{{ config('contacts.phone-2') }}</a></li>
    </ul>
</div>